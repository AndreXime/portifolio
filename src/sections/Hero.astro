---
import { ArrowDown, Github, Linkedin, Repeat } from "lucide-preact";
import { socialLinks } from "../content/social";
import { tsCode, goCode } from "../content/snippet";
import Snippets from "../components/Snippet.astro";
---

<section
  id="home"
  class="pt-30 pb-20 lg:py-22 min-h-screen flex items-center bg-section1"
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 w-full">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
      <div>
        <p class="text-lg text-textMuted mb-2 font-medium">Olá, Meu nome é</p>
        <h1
          class="text-5xl sm:text-7xl font-bold mb-4 leading-tight text-textMain tracking-tight"
        >
          André Ximenes
        </h1>
        <h2
          class="text-2xl sm:text-3xl text-textSecondary mb-8 font-medium leading-snug"
        >
          Sou desenvolvedor de software especializado em <span
            class="text-primary font-bold">TypeScript e Go</span
          >.
        </h2>

        <p
          class="text-lg text-textMuted mb-8 max-w-lg leading-relaxed border-l-4 border-primary/20 pl-4"
        >
          Desenvolvo sistemas focados em performance e manutenção, equilibrando
          rigor técnico e entrega. Aplico boas práticas para evitar débitos,
          garantindo que o software gere valor real em produção.
        </p>

        <div class="flex flex-wrap gap-4">
          <a
            href="#projetos"
            class="px-6 py-3 bg-primary text-white rounded-lg font-medium hover:bg-primaryDark transition-colors shadow-lg shadow-primary/25 flex items-center gap-2"
          >
            Ver Projetos <ArrowDown class="w-4 h-4" aria-hidden="true" />
          </a>

          <a
            href={socialLinks.github}
            target="_blank"
            rel="noreferrer"
            aria-label="Visitar meu perfil no GitHub"
            class="px-6 py-3 bg-github text-white rounded-lg font-medium hover:bg-githubHover transition-colors shadow-lg shadow-textMain/20 flex items-center gap-2"
          >
            <Github class="w-4 h-4" aria-hidden="true" /> GitHub
          </a>

          <a
            href={socialLinks.linkedin}
            target="_blank"
            rel="noreferrer"
            aria-label="Visitar meu perfil no LinkedIn"
            class="px-6 py-3 bg-linkedin text-white rounded-lg font-medium hover:bg-linkedinHover transition-colors shadow-lg shadow-info/20 flex items-center gap-2"
          >
            <Linkedin class="w-4 h-4" aria-hidden="true" /> LinkedIn
          </a>
        </div>
      </div>

      <div class="relative lg:block group perspective-[1000px] h-full">
        <div
          id="snippet-container"
          class="relative group perspective-1000 h-full cursor-pointer touch-manipulation"
          role="button"
          tabindex="0"
          aria-label="Alternar entre código TypeScript e Go"
        >
          <div
            id="snippet-card"
            class={`relative transition-all duration-700 transform-3d h-full`}
          >
            <div
              class="backface-hidden bg-codeBg rounded-xl border border-codeBorder/80"
            >
              <div
                class="flex justify-between items-center px-4 py-3 bg-codeHeader border-b border-codeBorder/80 rounded-xl"
              >
                <div class="flex gap-2">
                  <div class="w-3 h-3 rounded-full bg-error/80"></div>
                  <div class="w-3 h-3 rounded-full bg-warning/80"></div>
                  <div class="w-3 h-3 rounded-full bg-success/80"></div>
                </div>
                <div class="text-xs text-textSubtle font-mono">
                  developer.ts
                </div>
                <div
                  class="flex items-center gap-1 text-xs text-primary bg-primary/10 px-2 py-0.5 rounded border border-primary/20"
                >
                  <Repeat class="w-3 h-3" /> Flip
                </div>
              </div>
              <div class="p-4 pt-0">
                <Snippets lang={"ts"} code={tsCode} />
              </div>
            </div>

            <div
              class="absolute inset-0 backface-hidden transform-[rotateY(180deg)] bg-codeBg rounded-xl border border-codeBorder/80"
            >
              <div
                class="flex justify-between items-center px-4 py-3 bg-codeHeader border-b border-codeBorder/80 rounded-xl"
              >
                <div class="flex gap-2">
                  <div class="w-3 h-3 rounded-full bg-error/80"></div>
                  <div class="w-3 h-3 rounded-full bg-warning/80"></div>
                  <div class="w-3 h-3 rounded-full bg-success/80"></div>
                </div>
                <div class="text-xs text-textSubtle font-mono">
                  developer.go
                </div>
                <div
                  class="flex items-center gap-1 text-xs text-primary bg-primary/10 px-2 py-0.5 rounded border border-primary/20"
                >
                  <Repeat class="w-3 h-3" /> Flip
                </div>
              </div>
              <div class="p-4 pt-0">
                <Snippets lang={"go"} code={goCode} />
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <script is:inline type="module">
    function SetupCard() {
      const container = document.getElementById("snippet-container");
      const card = document.getElementById("snippet-card");
      let isFlipped = false;

      function flipCard() {
        isFlipped = !isFlipped;

        if (isFlipped) {
          card?.classList.add("[transform:rotateY(180deg)]");
        } else {
          card?.classList.remove("[transform:rotateY(180deg)]");
        }
      }

      container?.addEventListener("click", flipCard);
      container?.addEventListener("keydown", (e) => {
        if (e.key === "Enter" || e.key === " ") {
          e.preventDefault();
          flipCard();
        }
      });
    }
    SetupCard();
  </script>
</section>
