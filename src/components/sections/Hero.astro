---
import Container from "../ui/Container.astro";
import Button from "../ui/Button.astro";
import AccentBar from "../ui/AccentBar.astro";
import Reveal from "../ui/Reveal.astro";
import { site } from "../../content/site";

const { hero } = site;
const headlineParts = hero.headline.split(hero.headlineAccent);
---

<section
  id="inicio"
  class="border-b border-border bg-background py-20 md:py-28 lg:py-40"
  aria-labelledby="hero-heading"
>
  <Container class="relative">
    <Reveal staggerMs={100}>
      <AccentBar class="mb-8 md:mb-10" />
      <h1
        id="hero-heading"
        class="font-display max-w-[18ch] text-5xl font-semibold leading-[1.05] tracking-[-0.06em] text-foreground md:text-6xl lg:text-7xl"
      >
        {headlineParts[0]}
        <span class="text-accent">{hero.headlineAccent}</span>
        {headlineParts[1]}
      </h1>

      <div
        class="mt-8 max-w-2xl text-base leading-[1.75] text-foreground-variant md:text-lg md:leading-relaxed"
      >
        Olá, meu nome é <span class="font-bold text-foreground"
          >{site.authorName}</span
        >. {hero.intro}
      </div>

      <div
        class="mt-12 flex flex-col gap-4 sm:flex-row sm:flex-wrap sm:items-center sm:gap-8"
      >
        <Button href="#projetos" variant="primary" showArrow>
          Ver projetos
        </Button>
        <Button href="#contato" variant="secondary"> Entrar em contato </Button>
      </div>
    </Reveal>

    <dl
      class="mt-20 grid grid-cols-1 gap-10 border-t border-border pt-12 sm:grid-cols-2 md:grid-cols-3 md:gap-8"
    >
      <Reveal staggerMs={0}>
        <dt
          class="font-mono text-sm uppercase tracking-[0.15em] text-muted-foreground"
        >
          Stack
        </dt>
        <dd class="mt-2 font-mono text-sm text-foreground">
          {hero.stackLine}
        </dd>
      </Reveal>
      <Reveal staggerMs={80}>
        <dt
          class="font-mono text-sm uppercase tracking-[0.15em] text-muted-foreground"
        >
          {hero.availabilityLabel}
        </dt>
        <dd class="mt-2 font-mono text-sm text-foreground">
          {hero.availabilityValue}
        </dd>
      </Reveal>
      <Reveal staggerMs={160}>
        <dt
          class="font-mono text-sm uppercase tracking-[0.15em] text-muted-foreground"
        >
          Localização
        </dt>
        <dd class="mt-2 font-mono text-sm text-foreground">
          {hero.locationValue}
        </dd>
      </Reveal>
    </dl>
  </Container>
</section>
