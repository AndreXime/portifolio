---
interface Props {
  class?: string;
  element?: "div" | "section" | "header" | "footer" | "main";
}

const { class: className = "", element: Tag = "div" } = Astro.props;
---

<Tag
  class:list={["mx-auto w-full max-w-6xl px-5 md:px-10 lg:px-14", className]}
>
  <slot />
</Tag>
