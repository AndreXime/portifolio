---
interface Props {
  class?: string;
  as?: "div" | "section" | "header" | "footer" | "main";
}

const { class: className = "", as: Tag = "div" } = Astro.props;
---

<Tag class:list={["mx-auto w-full max-w-6xl px-5 md:px-10 lg:px-14", className]}>
  <slot />
</Tag>
