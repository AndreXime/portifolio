---
interface Props {
  class?: string;
  as?: "div" | "section" | "header" | "footer" | "main";
}

const { class: className = "", as: Tag = "div" } = Astro.props;
---

<Tag class:list={["mx-auto w-full max-w-5xl px-6 md:px-12 lg:px-16", className]}>
  <slot />
</Tag>
